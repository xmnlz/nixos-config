{ pkgs, ... }:

{
  # wayland.windowManager.hyprland = {
  #   systemd = {
  #     enable = true;
  #     variables = [ "--all" ];
  #   };
  # };
  #
  # xdg.configFile."hypr/hyprland.conf".source = ./hyprland.conf;
}
